# راهنمای اپراتور و مهندس شیفت — Steam Mixer Calculator v3

**اپلیکیشن:** Steam Mixer Calculator v3 (اندروید)
**موتور محاسبات:** فرمول صنعتی IAPWS-IF97 از طریق کتابخانه `com.hummeling:if97` (LGPL)
**مخاطب:** اپراتورهای بویلر، مهندسان شیفت، مهندسان راه‌اندازی و فرآیند
**نسخه سند:** 1.0 — سپتامبر 2026
**سند اصلی:** `OPERATOR_GUIDE.md` (نسخه انگلیسی — برای مقایسه و ارجاع)

> ⚠️ **هشدار ایمنی.** این اپلیکیشن یک *ابزار برآورد مهندسی* است. این برنامه **جایگزین**
> مستندات سازنده بویلر (OEM)، منطق کنترل DCS پست، یا دستورالعمل‌های بهره‌برداری سایت
> **نمی‌باشد**. هرگز صرفاً بر اساس این اپلیکیشن، کنترل آب اسپری یا شارژ مشعل‌ها را تغییر
> ندهید — همیشه با ابزار دقیق پست کنترل کنید و فرآیند مدیریت تغییر (MOC) سایت خود را
> دنبال نمایید.

---

## 1. هدف و دامنه کاربرد

این اپلیکیشن رفتار **خنک‌کاری با آب اسپری (Attemperation)** سوپرهیتر پلاتن را محاسبه
می‌کند. این برنامه به سه سؤال عملیاتی پاسخ می‌دهد:

| # | سؤال | صفحه |
|---|------|------|
| 1 | *«می‌خواهم خروجی روی X درجه سانتی‌گراد باشد — چه مقدار آب اسپری لازم است؟»* | **حالت A — اسپری موردنیاز** |
| 2 | *«Y تن بر ساعت اسپری تزریق می‌کنم — دمای خروجی واقعی چند درجه می‌شود؟»* | **حالت B — اثر اسپری** |
| 3 | *«بعد از روشن‌کردن مشعل، فلز پلاتن چقدر سریع گرم می‌شود و تقاضای اسپری چگونه رشد می‌کند؟»* | **شبیه‌سازی گذرا (Transient)** |

صفحه چهارم یعنی **موازنه انرژی (Energy Balance)** زنجیره آنتالپی پشت هر نتیجه را نشان
می‌دهد تا ببینید برنامه *چرا* آن عدد را تولید کرده است.

همه محاسبات **روی خود گوشی و به‌صورت آفلاین** انجام می‌شود — هیچ اتصال اینترنتی استفاده
یا لازم نیست. خواص بخار و آب از فرمول استاندارد بین‌المللی **IAPWS-IF97** گرفته می‌شود
(همان مبنایی که نرم‌افزارهای تجاری جدول بخار استفاده می‌کنند)، نه از میان‌برهای ساده‌شده
Cp ثابت.

---

## 2. شروع سریع (استفاده در سالن شیفت)

1. صفحه متناسب با سؤال خود را از تب‌های پایین انتخاب کنید.
2. **همه** فیلدها را وارد کنید — همه فیلدها اجباری هستند، از جمله *دمای اولیه فلز*.
3. دکمه **CALCULATE** را بزنید. کارت نتیجه، عدد را **همراه با کامل‌ترین مجموعه فرضیات و
   هشدارها** نشان می‌دهد. هرگز عدد را بدون فرضیاتش یادداشت نکنید.
4. اگر هشداری ظاهر شد (مثلاً *هشدار شوک حرارتی* یا *بخار تر*)، قبل از اقدام بر اساس
   نتیجه، بخش 8 را بخوانید.

---

## 3. مرور حالت‌های محاسباتی

| حالت | نوع | شما وارد می‌کنید | برنامه حل می‌کند | تکرار |
|------|-----|------------------|-------------------|-------|
| **A — اسپری موردنیاز** | معکوس | دمای هدف خروجی | دبی آب اسپری لازم | ندارد (فرم بسته) |
| **B — اثر اسپری** | مستقیم | دبی آب اسپری | دمای خروجی حاصل | ندارد (معادله معکوس IF97) |
| **گذرا (Transient)** | شبیه‌سازی گام‌زمانی | داده کامل مشعل + فلز | دمای فلز و تقاضای اسپری بر حسب زمان | گام‌برداری اویلر (dt) |

### واحدهای اندازه‌گیری (در کل اپلیکیشن یکسان)

| کمیت | واحد |
|------|------|
| دبی جرمی | t/h (تن بر ساعت) |
| فشار | bar (مطلق) |
| دما | °C |
| آنتالپی ویژه | kJ/kg |
| آهنگ حرارت | MW |
| جرم فلز | kg |
| گرمای ویژه | J/(kg·K) |
| زمان | ثانیه |

تبدیل‌های مفید: `1 t/h = 0.2778 kg/s`؛ `Q [MW] = ṁ [t/h] × Δh [kJ/kg] / 3600`.

---

## 4. مرجع پارامترهای ورودی

این فیلدها در همه صفحات محاسباتی ظاهر می‌شوند. **همه اجباری هستند.**

| پارامتر | نماد | محدوده مجاز (اپ) | مقدار معمول | توضیحات |
|---------|------|-------------------|--------------|---------|
| دبی جرمی بخار | ṁ_s | > 0 | 600 t/h | دبی بخار اصلی عبوری از پلاتن |
| فشار بخار | P | 20 – 170 bar | 167 bar | فشار مطلق در محل اسپری |
| دمای ورودی بخار | T_s | 200 – 560 °C | 540 °C | باید سوپرهیت باشد (به‌طور خودکار بررسی می‌شود) |
| دمای هدف خروجی | T_out | ≤ T_s | 520 °C | فقط حالت A و گذرا |
| دبی آب اسپری | ṁ_w | ≥ 0 و ≤ 2 × ṁ_s | 10 t/h | فقط حالت B؛ سقف 2× برای جلوگیری از خطای ورودی است |
| دمای آب اسپری | T_w | 0 – 600 °C و < T_sat(P) | 230 °C | باید **مایع ساب‌کول (زیراشباع)** باشد — بررسی خودکار |
| دمای اولیه فلز پلاتن | T_m0 | 0 – 700 °C | 540 °C | شرایط شروع فلز؛ هشدار شوک حرارتی را نیز فعال می‌کند |
| جرم فلز پلاتن | M | > 0 | 62 600 kg | فقط حالت گذرا |
| گرمای ویژه فلز | Cp | > 0 | 500 J/(kg·K) | مقدار فولاد آلیاژی؛ فقط حالت گذرا |
| حرارت هر مشعل | Q_burner | > 0 | 40 MW | فقط حالت گذرا |
| تعداد مشعل‌ها | N | ≥ 1 | 4 | فقط حالت گذرا |
| کسر حرارت مشعل به پلاتن | η | 0.0 – 1.0 | 0.07 | کسری از حرارت مشعل که توسط پلاتن جذب می‌شود |
| گام زمانی | dt | > 0 | 10 s | فقط حالت گذرا |
| مدت شبیه‌سازی | — | > 0 | 600 s | فقط حالت گذرا |

**چرا دمای اولیه فلز در همه‌جا اجباری است، حتی برای حالت‌های پایا؟**
چون یک محاسبه اختلاط پایا هیچ چیزی درباره وضعیت حرارتی فلز نمی‌گوید. ثبت این مقدار
(الف) وضعیت پست را در لحظه تهیه برآورد مستند می‌کند و (ب) *هشدار شوک حرارتی* را فعال
می‌کند (بخش 8.3). این مقدار هرگز به‌صورت پنهان نتیجه شما را تغییر نمی‌دهد.

---

## 5. حالت A — اسپری موردنیاز (محاسبه معکوس)

### 5.1 چه کاری انجام می‌دهد
با داشتن شرایط بخار و دمای خروجی مطلوب، دبی آب اسپری لازم را در نقطه اختلاط محاسبه
می‌کند، با فرض اختلاط آدیاباتیک در فشار ثابت.

### 5.2 فرمول

```
h_s = h(P, T_s)          آنتالپی بخار        (IF97 ناحیه 2)
h_w = h(P, T_w)          آنتالپی آب اسپری    (IF97 ناحیه 1، مایع فشرده)
h_out = h(P, T_target)   آنتالپی هدف         (IF97 ناحیه 2)

موازنه انرژی:  ṁ_s·h_s + ṁ_w·h_w = (ṁ_s + ṁ_w)·h_out

حل برای دبی اسپری:

        ṁ_w = ṁ_s · (h_s − h_out) / (h_out − h_w)          ← فرم بسته، بدون تکرار
```

### 5.3 مثال‌های حل‌شده (محاسبه‌شده با موتور IAPWS-IF97)

**مثال A1** — اسپری بخار اصلی معمول:

| ورودی | مقدار |
|-------|-------|
| دبی بخار | 600 t/h |
| فشار | 167 bar (T_sat = 350.8 °C) |
| دمای ورودی بخار | 540 °C |
| دمای هدف خروجی | 520 °C |
| دمای آب اسپری | 230 °C |

زنجیره: `h_s = 3404.3`، `h_out = 3347.0`، `h_w = 993.4 kJ/kg`

```
ṁ_w = 600 × (3404.3 − 3347.0) / (3347.0 − 993.4)
    = 600 × 57.3 / 2353.6
    = 14.61 t/h          دبی ترکیبی خروجی = 614.6 t/h
```

**مثال A2** — شرایط کلاس ری‌هیتر: 350 t/h، 100 bar (T_sat = 311.0 °C)،
480 → 460 °C، اسپری 180 °C → `h_s = 3322.9`، `h_out = 3269.5`، `h_w = 767.8`
→ اسپری **7.47 t/h**، دبی ترکیبی 357.5 t/h.

**مثال A3** — پلاتن فشار پایین/IP: 200 t/h، 50 bar (T_sat = 263.9 °C)،
400 → 380 °C، اسپری 150 °C → `h_s = 3196.6`، `h_out = 3146.8`، `h_w = 635.1`
→ اسپری **3.96 t/h**، دبی ترکیبی 204.0 t/h.

### 5.4 بررسی‌های خودکار ورودی (حالت A)
- دبی بخار باید مثبت باشد.
- دمای هدف باید ≤ دمای ورودی بخار باشد.
- بخار در (P, T_s) باید **سوپرهیت** باشد — در غیر این صورت با نمایش T_sat محلی رد می‌شود.
- آب اسپری باید **مایع ساب‌کول** باشد (T_w < T_sat در فشار P) — در غیر این صورت رد می‌شود.
- اگر آنتالپی آب اسپری ≥ آنتالپی هدف باشد، خنک‌کاری از نظر فیزیکی غیرممکن است —
  به‌صورت نیاز اسپری بی‌نهایت همراه با هشدار گزارش می‌شود.

---

## 6. حالت B — اثر اسپری (محاسبه مستقیم)

### 6.1 چه کاری انجام می‌دهد
با داشتن دبی اسپری که واقعاً قصد تزریق آن را دارید، دمای خروجی حاصل و افت دما را محاسبه
می‌کند. این حالتِ «اگر این مقدار اسپری بزنم چه می‌شود» است — هیچ دمای هدفی در کار نیست.

### 6.2 فرمول

```
h_out = (ṁ_s·h_s + ṁ_w·h_w) / (ṁ_s + ṁ_w)     آنتالپی نقطه اختلاط

T_out = T(P, h_out)                            معادله معکوس IF97 — فراخوانی مستقیم
ΔT    = T_s − T_out
```

دمای خروجی از *معادله معکوس* رسمی IF97 یعنی `T(P, h)` می‌آید — نه از تکرار و نه از
تقریب Cp — بنابراین حالت B در دقت صنعتی IF97 دقیق است.

### 6.3 مثال‌های حل‌شده (600 t/h، 167 bar، 540 °C، اسپری 230 °C)

| دبی اسپری | h_out (kJ/kg) | T_out (°C) | ΔT (K) |
|-----------:|--------------:|-----------:|-------:|
| 0 t/h | 3404.3 | 540.0 (ورودی) | 0.0 |
| 10 t/h | 3364.8 | 526.1 | 13.9 |
| 25 t/h | 3307.8 | 506.7 | 33.3 |
| 60 t/h | 3185.1 | 467.3 | 72.7 |

*خواندن جدول:* اولین 10 t/h اسپری حدود 14 کلوین خنک‌کاری می‌خرد؛ رفتن از 10 به 25 t/h
(15 t/h اضافی) حدود 19 کلوین دیگر می‌خرد. تقاضای اسپری **غیرخطی** است — به همین دلیل
حدس‌زدن از روی یک نقطه داده قبلی اغلب گمراه‌کننده است.

### 6.4 بررسی‌های خودکار ورودی (حالت B)
- دبی اسپری باید نامنفی و ≤ 2 × دبی بخار باشد (مقادیر بزرگ‌تر به‌عنوان خطای احتمالی
  ورودی رد می‌شوند).
- همان بررسی‌های بخار سوپرهیت و اسپری ساب‌کول مانند حالت A.
- اسپری صفر مجاز است و به‌سادگی دمای ورودی را با یک هشدار توضیحی برمی‌گرداند.

---

## 7. شبیه‌سازی گذرا (روشن‌شدن مشعل / اینرسی حرارتی پلاتن)

### 7.1 مدل فیزیکی

جرم فلزی سوپرهیتر پلاتن یک ظرفیت حرارتی واقعی است که در مقیاس زمانی خودش پاسخ می‌دهد.
مدل، پلاتن را به‌صورت جرمی با دمای یکنواخت (Lumped) در نظر می‌گیرد:

```
ظرفیت حرارتی:          C = M · Cp                        [kJ/K]

حرارت مشعل به پلاتن:   Q_in = η · N · Q_burner           [MW]

حذف حرارت با اسپری:    Q_spray = ṁ_w · (h_target − h_w) / 3600    [MW]

حرارت خالص:            Q_net = Q_in − Q_spray            [MW]

موازنه انرژی فلز:      dT_metal/dt = Q_net · 1000 / C    [K/s]

انتگرال‌گیری:          T(t+dt) = T(t) + Q_net · 1e6 · dt / (M·Cp)   (اویلر صریح)
```

در هر گام زمانی، مدل فرض می‌کند **بخار خارج‌شده از پلاتن در دمای فلز** است (انتقال
حرارت ساده‌شده)، دبی اسپری‌ای را محاسبه می‌کند که دمای هدف خروجی را حفظ کند، آن حرارت
را از فلز کسر می‌کند و یک گام جلو می‌رود.

### 7.2 مثال حل‌شده — ورودی‌های پیش‌فرض، روشن‌شدن از دمای فلز 540 °C

600 t/h بخار، 167 bar، هدف 520 °C، اسپری 230 °C، M = 62 600 kg،
Cp = 500 J/(kg·K)، 4 مشعل × 40 MW، η = 0.07 (یعنی Q_in = 11.2 MW)، dt = 10 s.

| زمان | دمای فلز (°C) | تقاضای اسپری (t/h) | حرارت اسپری (MW) | حرارت خالص (MW) |
|-----:|----------------:|-------------------:|----------------:|--------------:|
| 0 s | 540.00 | 14.61 | 9.55 | +1.65 |
| 60 s | 542.19 | 16.18 | 10.58 | +0.62 |
| 120 s | 543.01 | 16.77 | 10.97 | +0.23 |
| 300 s | 543.48 | 17.11 | 11.19 | +0.01 |
| 600 s | 543.51 | 17.13 | 11.20 | +0.00 |

*خواندن جدول:* با شروع از دمای فلز برابر دمای ورودی بخار، تقاضای اسپری با انباشته‌شدن
حرارت نامتوازن مشعل در فلز، از 14.6 به حدود 17.1 t/h **افزایش** می‌یابد و سپس تثبیت
می‌شود. نمودار روی صفحه دقیقاً همین منحنی‌ها را رسم می‌کند.

### 7.3 کاربردهای حالت گذرا
- برآورد سرعت رشد تقاضای اسپری بعد از روشن‌شدن مشعل یا جهش بار.
- بررسی اینکه شیر کنترل اسپری شما **دامنه و سرعت پاسخ** کافی دارد.
- درک اینکه چرا یک برآورد پایا روی پلاتن سرد، تقاضا را چند دقیقه بعد *کم‌برآورد* می‌کند.

---

## 8. هشدارها و توصیه‌ها (شرایط دقیق فعال‌شدن)

هر کارت نتیجه، عدد را **به‌همراه** همه هشدارهای فعال‌شده نشان می‌دهد. این‌ها شرایطی
هستند که موتور بررسی می‌کند و عبارت دقیق فعال‌سازی آن‌ها:

### 8.1 هشدار بخار تر / اسپری بیش از حد
**فعال‌سازی:** آنتالپی خروجی پیش‌بینی‌شده از آنتالپی اشباع در فشار پایین‌تر باشد (حالت A)
یا دمای خروجی ≤ T_sat(P) باشد (حالت B).
**معنا:** در حال اسپری بیش از حد هستید — جریان خروجی بخار تر خواهد بود. بخار تر در مدار
سوپرهیتر خطر حمل قطرات آب (Carry-over)، فرسایش توربین و تنش حرارتی لوله‌ها را دارد.
مدل اختلاط پایا برای شرایط خروجی دوفازی **معتبر نیست**.

### 8.2 هشدارهای بدون خنک‌کاری / خنک‌کاری غیرممکن
- دمای هدف ≥ دمای ورودی بخار → *اسپری لازم نیست* (حالت A).
- آنتالپی آب اسپری ≥ آنتالپی هدف → *خنک‌کاری غیرممکن است*؛ نیاز اسپری بی‌نهایت گزارش می‌شود.

### 8.3 هشدار شوک حرارتی (همه حالت‌ها)
**فعال‌سازی:** `T_steam − T_initial_metal > 50 °C` (ثابت آستانه در اپلیکیشن).
**متن:** *«دمای بخار به‌طور قابل‌توجهی از دمای فعلی فلز پلاتن بالاتر است — انتظار یک
تأخیر پاسخ واقعی فراتر از این برآورد پایا را داشته باشید؛ به‌جای آن از مدل گذرا استفاده
کنید.»*
**معنا:** عدد اختلاط برای سمت بخار هنوز درست است، اما فلز پلاتن عقب می‌ماند و ممکن است
تنش حرارتی گذرا ببیند. برای کمّی‌کردن این تأخیر به صفحه گذرا بروید.

### 8.4 رد ورودی‌ها (توقف سخت)
| شرط | مربوط به |
|------|-----------|
| دبی بخار ≤ 0 | A، B |
| فشار خارج از 20–170 bar | A، B، گذرا |
| دمای بخار خارج از 200–560 °C | A، B، گذرا |
| دمای هدف > دمای بخار | A، گذرا |
| دبی اسپری < 0 یا > 2× دبی بخار | B |
| دمای اسپری خارج از 0–600 °C یا ≥ T_sat(P) | A، B |
| دمای فلز خارج از 0–700 °C | A، B، گذرا |
| بخار در ورودی سوپرهیت نباشد | A، B، گذرا |
| جرم فلز / Cp / حرارت مشعل ≤ 0، مشعل < 1 | گذرا |
| η خارج از 0.0–1.0، dt یا مدت ≤ 0 | گذرا |

ورودی رد‌شده یک کارت خطای قرمز با دلیل دقیق نشان می‌دهد — برنامه هرگز مقداری را
به‌صورت پنهان مقدار پیش‌فرض نمی‌کند.

---

## 9. صفحه موازنه انرژی

با زدن دکمه **View Full Energy Balance** روی هر نتیجه، گزارشی باز می‌شود شامل:

- **برچسب حالت** (A / B / گذرا) — کدام مدل عدد را تولید کرده است.
- **شرایط ورودی** — دبی بخار، فشار، دمای ورودی، دمای هدف، دما و دبی اسپری، دمای اولیه فلز.
- **آنتالپی‌های سمت بخار** — h_steam(P, T_in)، h_spray(P, T_w)، h_out/هدف.
- **نتیجه محاسبه** — دبی اسپری یا دمای خروجی همراه با افت دما.

این صفحه «چرا؟» شماست: از آن استفاده کنید وقتی باید عددی را در دفتر ثبت شیفت توجیه
کنید یا زنجیره اپ را با یک محاسبه موازنه حرارتی پست مقایسه کنید.

---

## 10. فرضیات و محدودیت‌های مدل

قبل از اعتماد به یک عدد، این‌ها را بدانید:

1. **اختلاط آدیاباتیک در فشار ثابت.** بدون تلفات حرارت از اسپری‌کن به محیط؛ بدون افت
   فشار در نقطه اسپری.
2. **تبخیر کامل اسپری در بخار سوپرهیت.** معادلات اختلاط فرض می‌کنند خروجی سوپرهیت است.
   حالت‌های بخار تر به‌صورت هشدار علامت‌گذاری می‌شوند، نه مدل‌سازی.
3. **اختلاط کامل و آنی.** بدون تأخیر تبخیر اسپری، بدون اثرات توزیع در طول هدر، بدون
   خستگی حرارتی خود نازل اسپری.
4. **دقت صنعتی IF97.** مقادیر خواص با استاندارد صنعتی IAPWS-IF97 منطبق‌اند
   (معمولاً ±0.05 % در ناحیه سوپرهیت) — نه دقت مرجع ترمودینامیکی (IAPWS-95).
5. **ساده‌سازی‌های مدل گذرا.** دمای فلز یکنواخت (بدون گرادیان داخلی)؛ بخار خروجی از
   پلاتن در دمای فلز فرض می‌شود؛ η ثابت در زمان؛ انتگرال‌گیری اویلر صریح (برای منحنی‌های
   هموار dt ≤ 10 s نگه دارید).
6. **حالت‌های پایا اینرسی فلز را کاملاً نادیده می‌گیرند** — دقیقاً برای همین است که
   هشدار شوک حرارتی و حالت گذرا وجود دارند.

---

## 11. راهنمای عملی برای بهره‌برداری

**از حالت A استفاده کنید وقتی** هدف کنترلی داده شده است (مثلاً «بعد از پلاتن 520 °C را
نگه دار») و ست‌پوینت اسپری لازم را می‌خواهید.

**از حالت B استفاده کنید وقتی** موقعیت/دبی شیر اسپری ثابت است (مثلاً کنترل دستی اضطراری)
و می‌خواهید پیش‌بینی کنید دمای خروجی کجا تثبیت می‌شود.

**دو حالت را متقابلاً بررسی کنید:** دبی اسپری خروجی حالت A را به حالت B بدهید — باید
دمای هدف اولیه خود را برگردانید. تست‌های واحد خود برنامه دقیقاً همین کار را می‌کنند؛
این یک بررسی صحت خوب در محل نیز هست.

**روشن‌شدن با پلاتن سرد:** اگر هشدار شوک حرارتی فعال شود، پاسخ پایا برای *الان*
صادق است اما تقاضا با گرم‌شدن فلز **رو به بالا رانده می‌شود** — جدول گذرا در بخش 7.2
را ببینید، جایی که 14.6 t/h در ده دقیقه تحت 11.2 MW حرارت نامتوازن مشعل به 17.1 t/h
تبدیل شد.

**حساسیت تقریبی** در 167 bar / 540 °C / اسپری 230 °C (از جدول حالت B):
هر **1 t/h** اسپری تقریباً **1.3–1.4 K** خنک‌کاری می‌دهد نزدیک این شرایط. این ضریب
*ثابت نیست* — وقتی بار یا فشار تغییر می‌کند دوباره محاسبه کنید.

**همیشه با DCS مقایسه کنید:** اپ فرض اختلاط آدیاباتیک بدون تلفات و بدون تلفات اثربخشی
دی‌سوپرهیتر دارد. نصب‌های واقعی معمولاً به اسپری *بیشتری* نسبت به محاسبه ایده‌آل
نیاز دارند (اغلب 5–15 % بیشتر) به دلیل تبخیر و اختلاط ناقص. از اپ برای برآورد و
بررسی صحت استفاده کنید، نه به‌عنوان منبع ست‌پوینت کنترل.

---

## 12. سؤالات متداول / عیب‌یابی

**س: چرا اپ دمای اسپری من را رد می‌کند؟**
آب اسپری باید در فشار اسپری‌کن مایع ساب‌کول باشد. در 167 bar، T_sat = 350.8 °C — دمایی
در یا بالای این مقدار نمی‌تواند به‌صورت مایع وجود داشته باشد.

**س: چرا نتیجه دبی اسپری خیلی بزرگ/بی‌نهایت نشان می‌دهد؟**
آنتالپی آب اسپری شما در یا بالای آنتالپی هدف است — با آن دمای اسپری هرگز به هدف
نمی‌رسید. هدف را پایین بیاورید، یا (از نظر فیزیکی) شرایط آب اسپری اشتباه است.

**س: چرا دمای اولیه فلز حتی برای یک محاسبه پایای سریع اجباری است؟**
این مقدار وضعیت پست را پشت برآورد مستند می‌کند و هشدار شوک حرارتی را فعال می‌کند.
هرگز عدد اختلاط را تغییر نمی‌دهد.

**س: اپ می‌گوید «بخار تر / اسپری بیش از حد». حالا چه؟**
دبی اسپری را کم کنید (حالت B) یا دمای هدف را بالا ببرید (حالت A). مدل اختلاط شرایط
خروجی دوفازی را کمّی نمی‌کند.

**س: می‌توانم برای ری‌هیتر از آن استفاده کنم؟**
بله، ریاضیات همان است — فشار، دبی‌ها و دماهای ری‌هیتر را وارد کنید.

---

## 13. مراجع و مجوز

- **IAPWS-IF97**، *Revised Release on the IAPWS Industrial Formulation 1997 for the
  Thermodynamic Properties of Water and Steam* (شامل معادلات معکوس
  T(P,h)، T(P,s) — که این اپ مستقیماً برای حالت B از آن‌ها استفاده می‌کند).
- **کتابخانه `com.hummeling:if97`** جاوا/کاتلین، LGPL 2.1 — پیاده‌سازی نواحی 1–5 IF97،
  خط اشباع و معادلات معکوس. همین مجوز Copyleft-دوست‌داشتنی دلیل استفاده اپ از آن
  به‌جای کتابخانه‌های پایتونی خانواده GPL است.
- تست‌های واحد اپ حالت A ↔ حالت B را متقابلاً اعتبارسنجی می‌کنند (دبی اسپری از A،
  دمای هدف را در B بازتولید می‌کند) و مدل گذرا را در برابر حالت تحلیلی بدون اسپری
  بررسی می‌کنند. 36 تست واحد، همه موفق.

---

*Steam Mixer Calculator v3 — ساخته‌شده برای کسانی که بویلر را اداره می‌کنند.*
